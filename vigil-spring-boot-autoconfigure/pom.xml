<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.vigil</groupId>
        <artifactId>vigil</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>vigil-spring-boot-autoconfigure</artifactId>
    <name>Vigil Spring Boot Autoconfigure</name>
    <description>Spring Boot autoconfiguration with @ConditionalOnBean wiring</description>

    <dependencies>
        <dependency>
            <groupId>io.vigil</groupId>
            <artifactId>vigil-core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.vigil</groupId>
            <artifactId>vigil-scheduler</artifactId>
        </dependency>
        <dependency>
            <groupId>io.vigil</groupId>
            <artifactId>vigil-lock-jdbc</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.vigil</groupId>
            <artifactId>vigil-lock-redis</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>io.vigil</groupId>
            <artifactId>vigil-checkpoint-jdbc</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
